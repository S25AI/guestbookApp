name: Deploy to VPS

on:
  push:
    branches:
      - main  # или master, если у тебя ветка называется так

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: DEBUG Check secret length
        run: |
          echo "Secret length: $(echo '${{ secrets.SSH_PRIVATE_KEY }}' | wc -c) chars"
          echo "First line: $(echo '${{ secrets.SSH_PRIVATE_KEY }}' | head -n 1)"
          echo "Last line: $(echo '${{ secrets.SSH_PRIVATE_KEY }}' | tail -n 1)"

      - name: Setup SSH Key
        run: |
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          printf '%s\n' "\${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_ed25519
          
          chmod 600 ~/.ssh/id_ed25519
          
          ssh-keyscan -H \${{ secrets.VPS_IP }} >> ~/.ssh/known_hosts
          chmod 644 ~/.ssh/known_hosts
          
          # Создаем конфиг, чтобы явно указать путь к ключу и тип
          cat > ~/.ssh/config << EOF
          Host *
            IdentityFile ~/.ssh/id_ed25519
            IdentitiesOnly yes
            StrictHostKeyChecking yes
          EOF
          chmod 600 ~/.ssh/config

          # Проверка валидности ключа
          echo "--- Права на файлы ---"
          ls -la ~/.ssh/
          echo "--- Проверяем валидность ключа ---"
          ssh-keygen -l -f ~/.ssh/id_ed25519

      - name: Deploy to Server
        run: |
          echo "=== Подключение к серверу ==="
          ssh -v ${{ secrets.VPS_USER }}@${{ secrets.VPS_IP }} << 'EOF'
            cd /home/deploy/my-app
            
            if [ ! -d ".git" ]; then
              echo "Клонируем репозиторий..."
              git clone git@github.com:S25AI/guestbookApp.git .
            else
              echo "Обновляем существующий репозиторий..."
              git pull origin main
            fi

            echo "Запуск docker compose..."
            docker compose up -d --build --remove-orphans
          EOF